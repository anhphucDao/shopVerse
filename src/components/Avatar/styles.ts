import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 8,
  },
  image: {
    width: 24,
    height: 24,
    borderRadius: 100,
    resizeMode: 'contain',
  },
});

export default styles;
