import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'relative',
    marginTop: -12,
  },
  icon: {
    position: 'absolute',
    top: 0,
    right: 0,
  },
  badge: {
    position: 'absolute',
    top: 12,
    right: 6,
  },
});

export default styles;
