import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    top: 80,
    right: 0,
  },
  text: {
    color: 'white',
    fontFamily: 'DM-Sans',
    fontSize: 12,
    fontWeight: 'medium',
    lineHeight: 20,
  },
  textUnder: {
    marginTop: 4,
    color: 'white',
    fontFamily: 'DM-Sans',
    fontSize: 12,
    fontWeight: 'medium',
  },
});

export default styles;
